#type: http_setting
#
#url_base: "http://hogefuga.com/piyo/"
#
#---
type: scenario
desc: HTTP test

actions:
- action: http.get
  url: http://hogefugapiyo.com/
  assertex:
   - true # なんらかの例外が発生することだけをチェックしたい場合
   - ex|classname == 'ConnectionError'

- action: http.get
  url: http://google.co.jp/
  assert:
   - res.status == 200

  #assertex:
  # - ex.__class__.__name__ == 'ConnectionError'
