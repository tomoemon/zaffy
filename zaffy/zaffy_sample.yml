
actions:
  const:
    config:
      MYSQL_HOST: localhost
      MYSQL_USER: testuser
      MYSQL_PASS: testpass

  http:
    config:
    preset:
      default:
        url: "http://yahoo.co.jp"
        params: {}
  sql:
    config:
      # そのアクション全体に共通する設定をここに書く
      connect_each_time: false
    preset:
      default:
        host: <<const.MYSQL_HOST>>
        user: <<const.MYSQL_USER>>
        pass: <<const.MYSQL_PASS>>

